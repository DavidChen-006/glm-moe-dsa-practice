"""MERGED — this class now lives in modeling_glm_moe_dsa.py (as GlmMoeDsaPreTrainedModel).

Kept as an empty stub because it was split out earlier for study, but keeping it separate
created a circular import (it referenced DecoderLayer/Attention from the modeling file).
The real GLM keeps everything in one modeling file, so it's merged there now. This file is
no longer imported by anything.
"""
